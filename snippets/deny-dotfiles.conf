## Deny access to Apache's .htaccess files.
##
location ~ /\.ht {
    deny all;
}

## Deny access to git files.
##
location ~ /\.git {
    deny all;
}

## Deny access to env files.
##
location ~* \.env$ {
    deny all;
}